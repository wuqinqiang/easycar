## conf

dirver: mysql
db:
  mysql:
    dbURL: mysql://root:root@127.0.0.1/test?charset=utf8&parseTime=True&loc=Local
    maxLifetime: 7200
    maxIdleConns: 20
    maxOpenConns: 10

  ## todo add more store drivers

server:
  ## easycar coordinator server port
  Addr: 8089
  ## backOff for retry
  backOff:
    ##  max delay time for retry,unit senconds
    maxDelay: 120
    ## max retry times when rm is not available
    maxRetries: 3
    factor: 2